DATABASE_URL="postgresql://user:password@localhost:5432/note_track"
JWT_SECRET="change-me-to-a-long-random-string"
JWT_EXPIRES_IN="7d"
PORT=3000
LANGUAGETOOL_API_URL="https://api.languagetool.org/v2/check"

# Vercel: set the same variables in Project → Settings → Environment Variables
# Use a pooled Postgres URL for serverless (Neon, Supabase pooler, Prisma Postgres, etc.)
